---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*.

```{r}
library(statsr)
library(dplyr)
library(ggplot2)
```
```{r}
load(url("https://stat.duke.edu/~mc301/data/hot_hand.RData"))
```

```{r}
kobe_streak<-calc_streak(kobe_basket$shot)
kobe_streak
```
```{r}
ggplot(kobe_streak,aes(x=length))+geom_histogram(binwidth = 1)
```


```{r}
coin_toss<-c("H","T")
```


```{r}
sim_fair_coin<-sample(coin_toss,100,replace = TRUE)

table(sim_fair_coin)
```
```{r}
normal_player_shots<-sample(c("H","M"),133, replace=TRUE, prob = c(.45,.55))

normal_calc_streak<-calc_streak(normal_player_shots)

normal_calc_streak

```
```{r}
ggplot(normal_calc_streak,aes(x=length))+geom_histogram(binwidth = 1)
```

